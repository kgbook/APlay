if(APLAY_BUILD_LINUX)
    set(APLAY_OSAL_PLATFORM_MODULE
        linux)
    set(APLAY_OSAL_PLATFORM_LIBRARIE
        linux)
elseif(APLAY_BUILD_ANDROID)
    set(APLAY_OSAL_PLATFORM_MODULE
        android)
    set(APLAY_OSAL_PLATFORM_LIBRARIE
        android)
elseif(APLAY_BUILD_HARMONY)
    set(APLAY_OSAL_PLATFORM_MODULE
        harmony)
    set(APLAY_OSAL_PLATFORM_LIBRARIE
        harmony)
endif()

set(LOCAL_CMAKE_LISTS_DIR "${CMAKE_CURRENT_SOURCE_DIR}/${APLAY_OSAL_PLATFORM_MODULE}")
set(LOCAL_CMAKE_LISTS_BINARY_DIR "${CMAKE_CURRENT_BINARY_DIR}/${APLAY_OSAL_PLATFORM_MODULE}")
include("${LOCAL_BUILD_SYSTEM_PATH}/internal/AddCMakeListDirectory.cmake")

include(${CLEAR_VARS})
set(LOCAL_MODULE osal)
set(LOCAL_INTERFACE_LIBRARIES
    ${APLAY_OSAL_PLATFORM_LIBRARIE})
include(${BUILD_INTERFACE_LIBRARY})
