if(APLAY_BUILD_LINUX)
    set(APLAY_OSAL_PLATFORM_MODULE linux)
elseif(APLAY_BUILD_ANDROID)
    set(APLAY_OSAL_PLATFORM_MODULE android)
elseif(APLAY_BUILD_HARMONY)
    set(APLAY_OSAL_PLATFORM_MODULE harmony)
endif()

include(${ALL_SUBDIR_CMAKELISTS})

include(${CLEAR_VARS})
set(LOCAL_MODULE osal)
set(LOCAL_INTERFACE_LIBRARIES ${APLAY_OSAL_PLATFORM_MODULE})
include(${BUILD_INTERFACE_LIBRARY})
