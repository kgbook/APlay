# Platform app entrypoints are intentionally not aggregated here.
#
# Linux is built from the repository root CMakeLists.txt via app/linux.
# Android is built from app/android as an independent Gradle project and imports
# the root CMake project only as a native dependency.
