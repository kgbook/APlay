set(APLAY_HARNESS_MDNS_OUTPUT_DIR ${CMAKE_BINARY_DIR}/harness/mdns)

include(${CLEAR_VARS})
set(LOCAL_MODULE aplay_harness_mdns_replay)
set(LOCAL_SRC_FILES mdns_replay.cpp)
set(LOCAL_OBJECT_LIBRARIES mdns)
set(LOCAL_RUNTIME_OUTPUT_DIRECTORY ${APLAY_HARNESS_MDNS_OUTPUT_DIR})
include(${BUILD_EXECUTABLE})

include(${CLEAR_VARS})
set(LOCAL_MODULE aplay_harness_mdns_announce)
set(LOCAL_SRC_FILES mdns_announce.cpp)
set(LOCAL_OBJECT_LIBRARIES mdns)
set(LOCAL_RUNTIME_OUTPUT_DIRECTORY ${APLAY_HARNESS_MDNS_OUTPUT_DIR})
include(${BUILD_EXECUTABLE})
